# =====================
# Server
# =====================
NODE_ENV=development
PORT=5000

# JWT — đổi secret trước khi deploy production
JWT_SECRET=change-me-in-production
JWT_EXPIRE=24h

# =====================
# PostgreSQL
# =====================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=web_giai_dau
